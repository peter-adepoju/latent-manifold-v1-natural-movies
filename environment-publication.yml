name: v1-natural-movie-publication
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - numpy>=1.24
  - pandas>=2.0
  - scipy>=1.10
  - scikit-learn>=1.2
  - matplotlib>=3.7
  - joblib>=1.3
  - pyyaml>=6.0
  - tqdm>=4.66
  - statsmodels>=0.14
  - networkx>=3.1
  - pip
  - pip:
      - pytest
      - nbformat
      # Optional heavyweight extensions:
      # - allensdk
      # - torch
      # - torchvision
      # - timm
      # - opencv-python
      # - scikit-image
      # - ripser
      # - cebra
